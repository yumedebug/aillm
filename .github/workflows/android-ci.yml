name: Android CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: '9.1.0'

    - name: Build Debug APK
      id: build
      run: gradle assembleDebug --no-daemon --stacktrace 2>&1 | tee build-output.txt || true

    - name: Report build result
      if: always()
      run: |
        if ls app/build/outputs/apk/debug/*.apk 2>/dev/null; then
          echo "## Build SUCCEEDED" >> $GITHUB_STEP_SUMMARY
          echo "APK found at app/build/outputs/apk/debug/" >> $GITHUB_STEP_SUMMARY
        else
          echo "## Build FAILED" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          tail -200 build-output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
        fi

    - name: Fail if no APK
      run: |
        if ! ls app/build/outputs/apk/debug/*.apk 2>/dev/null; then
          exit 1
        fi

    - name: Upload build log
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-output-log
        path: build-output.txt
        retention-days: 7

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: aillm-debug-apk
        path: app/build/outputs/apk/debug/*.apk
        retention-days: 30
