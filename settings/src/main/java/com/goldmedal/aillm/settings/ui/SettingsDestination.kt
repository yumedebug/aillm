package com.goldmedal.aillm.settings.ui

/** Secondary destinations reachable from Settings. The host app maps these to routes. */
enum class SettingsDestination {
    APPEARANCE,
    AI,
    MODELS,
    MEMORY,
    FILES,
    STORAGE,
    ADVANCED,
    ONLINE_SOURCES,
    ABOUT
}
